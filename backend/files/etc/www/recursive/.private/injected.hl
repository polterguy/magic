
/*
 * Example of mixin file that cannot be directly resolved since it's
 * within a folder containing a ".". This file is intended for being used as
 * a "component" rather, and consumed by other mixin files, as an HTML snippet
 * rendered using [mixin] from the codebehind of another Hyperlambda file.
 */


url
   request.url
   return:x:-

arg1
   return:x:@.arguments/*/arg1