
/*
 * Example of mixin file that injected another mixin file in its codebehind
 * Hyperlambda file, passing in arguments to the included mixin file.
 */


inject
   mixin:/etc/www/recursive/.private/injected.html
      arg1:Argument 1
   return:x:-
