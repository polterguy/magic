// Deletes one item from your DATABASE_NAME database and TABLE_NAME table with the id of WHATEVER_ID_HERE.
data.connect:DATABASE_NAME
   database-type:sqlite

   // Deleting item from database.
   data.delete
      database-type:sqlite
      table:TABLE_NAME
      where
         and
            id.eq:x:@WHATEVER_ID_HERE
