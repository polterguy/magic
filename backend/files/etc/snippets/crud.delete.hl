// Use Hyperlambda to delete all records from the tbl table in the db database where name equals 'John Doe'
data.connect:db
   data.delete
      table:tbl
      where
         and
            name.eq:John Doe
