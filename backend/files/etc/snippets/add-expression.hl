// Use Hyperlambda to add all nodes from the [.source] node's children collection into the [.destination] node's children collection | Add all nodes into [.destination] using a source expression with Hyperlambda | Use Hyperlambda expression to add nodes | Use expression to add nodes in Hyperlambda
.source
   foo1:bar1
   foo2:bar2
   foo3:bar3
.destination
add:x:@.destination
   get-nodes:x:@.source/*
