// Create two threads that retrieves two URLs using HTTP GET invocations, for then to wait for both threads to finish
join
   fork
      http.get:"https://gaiasoul.com"
   fork
      http.get:"https://servergardens.com"
