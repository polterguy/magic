// Use Hyperlambda to replace 'foo' in [.src] with 'bar' and assign the result back to [.src] | String replace | Replace string sections
.src:howdy foo hello foo this will be foo replaced
set-value:x:@.src
   strings.replace:x:@.src
      .:foo
      .:bar
