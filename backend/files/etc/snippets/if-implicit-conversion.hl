
/*
 * This example demonstrates implicit conversion for non-null object to
 * boolean. If an object is not bool, but it is non-null, during boolean
 * conversion it will automatically be defined as "true".
 */
.result
.foo:whatever
if:x:-
   set-value:x:@.result
      .:Yup!
else
   set-value:x:@.result
      .:Nope!
