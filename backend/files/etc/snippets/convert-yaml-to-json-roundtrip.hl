// Use Hyperlambda to convert the specified YAML to JSON and back to YAML again | Convert to and from YAML to lambda | Convert YAML using Hyperlambda | Convert from YAML
.yaml:@"foo: howdy
bar: jo
howdy: !!bool true
children:
- howdy
- some
"

// Converting from YAML to JSON
yaml2json:x:@.yaml

// Converting back to JSON again from YAML
json2yaml:x:-