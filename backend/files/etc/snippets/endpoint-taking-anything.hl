// Create Hyperlambda endpoint that can be given any arguments that returns result equal to success to caller | Hyperlambda endpoint taking any arguments | Any arguments can be specified to endpoint
.arguments:*
return
   result:success
