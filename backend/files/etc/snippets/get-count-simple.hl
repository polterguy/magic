// Use Hyperlambda to count how many nodes can be found inside of our [.data] object and assign the count to .result | Count nodes
.result
.data
   foo1:bar1
   foo2:bar2
   foo3:bar3
   foo4:bar4
set-value:x:@.result
   get-count:x:@.data/*
