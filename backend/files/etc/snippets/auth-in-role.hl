// Use Hyperlambda to set the value of [.foo] to boolean true if the authenticated user belongs to the role of admin or root
.foo
if
   auth.ticket.in-role:admin, root
   .lambda
      set-value:x:@.foo
         .:bool:true
