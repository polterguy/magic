// Create a while loop that iterates from 1 to 20, logging the text 'Howdy from while' as it iterates
.no:int:0
while
   lt
      get-value:x:@.no
      .:int:20
   .lambda
      log.info:Howdy from while
      math.increment:x:@.no
