// Evaluates specified [.lambda] for as long as condition evaluates to true.
.no:int:0
while
   lt
      get-value:x:@.no
      .:int:20
   .lambda

      // Your lambda goes here.
      log.info:Howdy from while

      // Incrementing counter.
      math.increment:x:@.no
