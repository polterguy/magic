// Use Hyperlambda to convert the string value found in [.source] node's value to an integer and store it in [.destination]
.source:55
.destination
set-value:x:@.destination
   convert:x:@.source
      type:int
