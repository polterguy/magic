// Use Hyperlambda to create a thread that writes the text 'Hello from thread' to the server's log | Create a new thread using Hyperlambda | Create thread | Execute in parallel | Threading in Hyperlambda
fork
   log.info:Hello from thread
