// Use Hyperlambda to replace all characters that is not an alpha numeric character with the character '-'
.src:howdy foo hello foo this will be foo replaced
strings.replace-not-of:x:@.src
   .:abcdefghijklmnopqrstuvwxyz
   .:-
