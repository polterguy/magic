/*
 * Opens a database connection and executes some arbitrary SQL,
 * returning results as a record set.
 */
data.connect:DATABASE_NAME_HERE
   database-type:sqlite
   data.select:select * from TABLE_NAME_HERE
      database-type:sqlite
