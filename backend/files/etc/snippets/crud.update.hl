// Use Hyperlambda to update table tbl in database db and set name to John Doe where id equals 555
data.connect:DATABASE_NAME
   data.update
      table:TABLE_NAME
      values
         name:John Doe
      where
         and
            id.eq:int:555
