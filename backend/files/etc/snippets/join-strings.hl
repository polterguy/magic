// Hyperlambda snippet that joins all strings found at the expression and adds , between each string | Join strings using Hyperlambda | Hyperlambda joining 3 strings and adds , between each string | string.join with Hyperlambda | Joining strings
.data
   .:string1
   .:string2
   .:string3
strings.join:x:@.data/*
   .:,
