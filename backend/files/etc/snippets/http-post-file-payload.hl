// Use Hyperlambda to create an HTTP POST invocation towards microsoft.com that uses the file named README.md as its payload | Hyperlambda HTTP POST with file
http.post:"https://microsoft.com"
   filename:/README.md
