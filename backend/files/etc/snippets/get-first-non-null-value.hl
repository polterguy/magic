// Use Hyperlambda to create some Hyperlambda that returns the first non-null value from 3 arguments specified
.arguments
   foo1
   foo2:hello
   foo3
.res
set-value:x:@.res
   get-first-value:x:@.arguments/*