
/*
 * Example workflow executing sql-select action, for then
 * to return result of invocation.
 */

// Executes sql-select function.
execute:magic.workflows.function.execute
   name:sql-select
   filename:/misc/workflows/functions/sql-select.hl
   arguments
      sql:select name from roles order by name limit 2
return:x:-/*