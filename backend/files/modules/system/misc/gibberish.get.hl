
/*
 * Returns Cryptographically Secure random text (gibberish) to caller according to arguments specified.
 */
.description:Returns some Cryptographically Secure randomly generated text to caller according to specified arguments
.arguments
   min:long
   max:long


/*
 * Generating gibberish text.
 */
crypto.random
   min:x:@.arguments/*/min
   max:x:@.arguments/*/max


/*
 * Generating gibberish text.
 */
unwrap:x:+/*
return
   result:x:@crypto.random
