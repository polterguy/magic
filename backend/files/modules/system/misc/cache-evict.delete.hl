
/*
 * Evicts an item from the cache.
 */
.description:"Evicts an item from the cache."
.arguments
   id:string


/*
 * Ensuring only authorised users can access endpoint.
 */
auth.ticket.verify:root


/*
 * Evicting item from cache.
 */
cache.set:x:@.arguments/*/id
return
   result:success
