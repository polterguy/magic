
/*
 * Template for HTTP DELETE CRUD requests.
 */
.arguments
.type:crud-delete


/*
 * Invoking delete, making sure we add any specified ids.
 */
xxx.connect:{database}
   add:x:+/*/where/*/and
      get-nodes:x:@.arguments/*
   xxx.delete
      table:{table}
      where
         and
   unwrap:x:+/*
   return
      deleted-records:x:./-/-
