
/*
 * Pings backend, allowing the client side to verify the
 * backend is up running.
 */
.description:"Pings server, to verify connection is successfully established. Will return result equaling success if successful."
return-nodes
   result:success
