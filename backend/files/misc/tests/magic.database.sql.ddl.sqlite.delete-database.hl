verb:delete
url:/magic/system/sql/ddl/database?databaseType=sqlite&connectionString=generic&databaseName=test&generate=true
status:int:200
description:Verifies that SQLite correctly allows for invoking delete on database
response:"{\n  \"result\": \"/* SQLite does not support drop database syntax */\"\n}"
produces:application/json
