verb:get
url:/magic/modules/system/crudifier/columns?databaseType=non-existent-database-type&database=magic&table=users
status:int:400
description:Assumes that invoking endpoint with a non-existent database type results in the correct error response and status code
response:@"{
  ""message"": ""'non-existent-database-type' is not a valid enum of ['mysql', 'mssql']"",
  ""field"": ""databaseType""
}"
produces:application/json
