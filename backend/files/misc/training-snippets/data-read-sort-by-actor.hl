// Select 20 records from sakila database and actor table sorted by first_name descending, and return first_name and last_name columns
data.connect:sakila
   data.read
      table:actor
      columns
         first_name
         last_name
      order:first_name
      direction:desc
      limit:20
