// Create log entry if [.arg1] is not equal to [.arg2]
.arg1:foo
.arg2:foo

// Doing our comparison
if

   // The [eq] slot implies 'equals to'
   neq:x:@.arg1
      get-value:x:@.arg2
   .lambda

      // Match, creating our log item
      log.info:Matches!
