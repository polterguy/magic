// Use Hyperlambda to create an Hyperlambda endpoint that accepts a name and an email argument, where the email argument must be a valid email | Validate input is email | Make sure argument is a valid email address
.arguments
   name:string
   email:string
validators.email:x:@.arguments/*/email
return
   result:success
