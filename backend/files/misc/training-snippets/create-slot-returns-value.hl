// Use Hyperlambda to create a new slot named my_slot that returns the integer value of 42, and then invoke the slot | Create a new slot returning a value | Create a function in Hyperlambda | Create function

// Creating our slot
slots.create:my_slot
   return-value:int:42

// Invoking our slot
signal:my_slot
