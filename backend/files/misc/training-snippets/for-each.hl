// Iterate through each child node in our [.data] lambda collection, and write its value to the log | Log all values from a list of nodes | For each loop creating a log entry
.data
   item1:Hello from Item1
   item2:Hello from Item2
   item3:Hello from Item3
for-each:x:@.data/*
   log.info:x:@.dp/#
