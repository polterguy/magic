// Connect to a database with the name of magic and return all values from roles and users tables
data.connect:magic
   data.select:"select * from roles; select * from users;"
      multiple-result-sets:bool:true
