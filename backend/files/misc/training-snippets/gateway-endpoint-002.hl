// Forward all arguments given to microsoft.com and return whatever microsoft.com returns to caller
.arguments:*
add:x:../*/http.post/*/payload
   get-nodes:x:@.arguments/*
http.post:"https://microsoft.com"
   convert:true
   payload
return-nodes:x:@http.get/*/content/*
