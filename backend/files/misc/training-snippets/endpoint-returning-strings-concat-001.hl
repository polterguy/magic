// Endpoint concatenating two strings
.arguments
   arg1:string
   arg2:string
strings.concat
   get-value:x:@.arguments/*/arg1
   get-value:x:@.arguments/*/arg2
unwrap:x:+/*
return
   result:x:@strings.concat
