
// Endpoint returning items from country table in sakila database with filtering on country to specify items to return with sorting requiring authentication to invoke
.arguments
   order:string
   direction:string
   country.country.like:string
   country.country.eq:string

// Verifying user is authorized to access endpoint.
auth.ticket.verify:root,admin

// Opening up our database connection.
data.connect:sakila

   // Parametrising our read invocation.
   add:x:./*/data.read
      get-nodes:x:@.arguments/*/order
      get-nodes:x:@.arguments/*/direction
   remove-nodes:x:@.arguments/*/order
   remove-nodes:x:@.arguments/*/direction
   add:x:./*/data.read/*/where/*
      get-nodes:x:@.arguments/*

   // Reading data from database.
   data.read
      table:country
      columns
         country.country_id
         country.country
         country.last_update
      where
         and

   // Returning result of above read invocation to caller.
   return-nodes:x:@data.read/*
