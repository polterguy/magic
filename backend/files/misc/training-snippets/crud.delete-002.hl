// Delete all records from the xyz table in the qwe database where created is older than 5 days
data.connect:qwe
   math.subtract
      date.now
      time
         days:5
   data.delete
      table:xyz
      where
         and
            created.lt:x:@math.subtract
