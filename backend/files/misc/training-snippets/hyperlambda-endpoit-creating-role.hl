// Endpoint inserting name and description arguments into roles table in magic database
.arguments
   name:string
   description:string
data.connect:magic
   data.create
      table:roles
      values
         name:x:@.arguments/*/name
         description:x:@.arguments/*/description
response.status.set:201
return
   result:Insert was a success