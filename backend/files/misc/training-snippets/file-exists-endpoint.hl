// Endpoint returning SUCCESS if filename argument is an existing file and FAILURE if not
.arguments
   filename:string

// Checking if file exists
if
   io.file.exists:x:@.arguments/*/filename
   .lambda
      return
         result:SUCCESS

// File didn't exist, returning FAILURE to caller
return
   result:FAILURE
