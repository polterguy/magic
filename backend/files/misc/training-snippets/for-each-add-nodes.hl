// Add all children of [.src] to all children of [.data]
.src
   foo1:bar1
   foo2:bar2
.data
   foo1:bar1
   foo2:bar2

// Iterating through each node in [.data]
for-each:x:@.data/*

   // Adding into currently iterated node all nodes found in [.src]
   add:x:@.dp/#
      get-nodes:x:@.src/*
