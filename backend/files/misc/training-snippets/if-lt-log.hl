// Create log entry if [.arg1] is less than [.arg2]
.arg1:value 1
.arg2:value 2

// Doing our comparison
if

   // The [eq] slot implies 'equals to'
   lt:x:@.arg1
      get-value:x:@.arg2
   .lambda

      // Match, creating our log item
      log.info:Matches!
