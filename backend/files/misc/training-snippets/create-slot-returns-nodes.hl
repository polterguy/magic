// Use Hyperlambda to create a new slot named my_slot2 that returns a lambda object, for then to invoke the slot | Create a function | Create a slot in Hyperlambda | Create a new slot

// Creating our slot
slots.create:my_slot2
   return
      lambda1:foo
      lambda2:bar

// Invoking our slot
signal:my_slot2
