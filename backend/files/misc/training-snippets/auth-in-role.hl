// Set the value of [.foo] to boolean true if the authenticated user belongs to the role of admin or root
.foo

// Conditional if statement
if
   // Checking if user belongs to either admin or root
   auth.ticket.in-role:admin, root
   .lambda

      // Setting the value of [.foo] to boolean true
      set-value:x:@.foo
         .:bool:true
