// Update table tbl in database db and set name to John Doe where id equals 555
data.connect:db
   data.update
      table:tbl
      values
         name:John Doe
      where
         and
            id.eq:int:555
