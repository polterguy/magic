// Endpoint returning the specified filename argument file to caller for root users
.arguments
   filename:string

// Ensuring user belongs to root role
auth.ticket.verify:root

// Loading file
io.file.load:x:@.arguments/*/filename
   
// Returning content of file to caller.
return:x:@io.file.load
