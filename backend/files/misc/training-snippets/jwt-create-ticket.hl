// Use Hyperlambda to create a JWT token with foo as username belonging to the roles of admin and root | Create JWT token | JWT token with Hyperlambda | Create JWT
auth.ticket.create
   username:foo
   roles
      .:admin
      .:root
