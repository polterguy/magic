// Scrape all paragraphs from aista.com and assign to [.result] as an array
.result
http.get:"https://aista.com"
html2lambda:x:@http.get/*/content
for-each:x:@html2lambda/**/p/*/\#text
   unwrap:x:+/*/*
   add:x:@.result
      .
         .:x:@.dp/#
