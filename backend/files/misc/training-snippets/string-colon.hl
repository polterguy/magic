// Use Hyperlambda to create a string containing a " character | Hyperlambda string | String with : character
.foo:"This is a string : with a colon character in it. It needs to be escaped since the : character normally declares the type"
