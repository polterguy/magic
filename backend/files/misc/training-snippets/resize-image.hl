// Endpoint loading /foo.png and resizing it to 640x480 pixels before returning it to client as png
.arguments
image.resize:/foo.jpg
   width:640
   height:480
   type:png
io.stream.save-file:/foo.png
   get-value:x:@image.resize
