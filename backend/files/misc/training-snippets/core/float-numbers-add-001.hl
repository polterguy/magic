// Create an endpoint that takes two floating point numbers, adds them together, and returns the result of the addition to the caller
.arguments
   number1:float
   number2:float

// Adding our numbers together
math.add
   get-value:x:@.arguments/*/number1
   get-value:x:@.arguments/*/number2

// Returning the result to caller
unwrap:x:+/*
return
   result:x:@math.add
