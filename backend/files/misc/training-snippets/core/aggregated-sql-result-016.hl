// Endpoint returning records in foo2 table towards zxc database having a name matching query argument
.arguments
   query:string
data.connect:zxc
   data.select:select * from foo2 where name like @query
      query:x:@.arguments/*/query
   unwrap:x:+/*
   return-nodes
      count:x:@data.select/*
