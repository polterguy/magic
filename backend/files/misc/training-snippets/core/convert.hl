// Use Hyperlambda to convert the string value found in [.source] node's value to an integer and store it in [.destination] | Convert from string to integer | Convert value to integer
.source:55
.destination

// Settings the value of our [.destination] node to the value of [.source] converting it to integer first
set-value:x:@.destination
   convert:x:@.source
      type:int
