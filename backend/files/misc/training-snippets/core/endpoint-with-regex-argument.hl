// Endpoint with regex validator confirming arg1 matches regular expression ^[a-z0-9]*$ returning OK
.arguments
   arg1:string
validators.regex:x:@.arguments/*/arg1
   regex:^[a-z0-9]*$
return
   result:OK
