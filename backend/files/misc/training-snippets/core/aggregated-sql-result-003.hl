// Joins actor with film and film_actor from movies database, and orders by film title
data.connect:movies
   data.select:@"select a.first_name || ' ' || a.last_name as actor, f.title as film
  from actor a
    inner join film_actor fa on a.actor_id = fa.actor_id
    inner join film f on f.film_id = fa.film_id
  order by f.title"
