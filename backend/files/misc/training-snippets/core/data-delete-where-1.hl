// Use Hyperlambda to delete all items from sakila's film table where title equals to 'Howdy world' | Delete records in database matching where | Delete records in database
data.connect:sakila
   data.delete
      table:film
      where
         and
            title.eq:Howdy world
