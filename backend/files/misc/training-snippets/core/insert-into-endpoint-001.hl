// endpoint inserting email and phone into address table | endpoint creating address record with email and name
.arguments
   email:string
   phone:string
data.connect:YOUR_DATABASE
   data.create
      table:address
      values
         email:x:@.arguments/*/email
         phone:x:@.arguments/*/phone

   // Returning success
   return
      result:Insert was a success
