// endpoint inserting adr1 and zip into accounting table | endpoint creating accounting record with adr1 and zip
.arguments
   adr1:string
   zip:string
data.connect:YOUR_DATABASE
   data.create
      table:accounting
      values
         adr1:x:@.arguments/*/adr1
         zip:x:@.arguments/*/zip

   // Returning success
   return
      result:Insert was a success
