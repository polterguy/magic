// Endpoint selecting name and title from actors table in crm with authentication and authorisation for root users
.arguments
auth.ticket.verify:root
data.connect:crm
   data.read
      table:actors
      columns
         name
         title

   // Returning result to caller
   return-nodes:x:@data.read/*
