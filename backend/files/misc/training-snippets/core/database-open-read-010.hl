// Connect to scanning database and select 10 records from factories table offset 30
data.connect:scanning

   // Read 10 records from factories offset 30
   data.read
      table:factories
      limit:10
      offset:30
