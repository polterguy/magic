
// Endpoint returning the value of expression leading to [.result]
.arguments

// We will return this referencing the node using an expression.
.result:this is the value

// Returning result.
unwrap:x:+/*
return
   result:x:@.result
