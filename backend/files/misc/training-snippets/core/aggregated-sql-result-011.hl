// Endpoint returning number of cloudlets per cluster
.arguments
data.connect:hub
   data.read
      table:cloudlets
      columns
         count(*)
            as:cloudlets
         cluster
      group
         cluster
   return-nodes:x:@data.read/*
