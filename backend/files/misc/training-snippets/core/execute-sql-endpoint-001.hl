// Endpoint executing SQL counting records in foo table in bar database and returning result to caller
.arguments
data.connect:bar
   data.scalar:select count(*) from foo
   unwrap:x:+/*
   return
      result:x:@data.scalar
