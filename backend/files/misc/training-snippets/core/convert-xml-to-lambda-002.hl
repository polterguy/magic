
// Convert [.data] XML to lambda

// XML string you want to convert.
.data:@"
<article>
 <title>Hello World</title>
</article>"

// Converts the above [.data] node's value from XML text to a lambda object.
xml2lambda:x:@.data
