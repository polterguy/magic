// Add all nodes from the [.source] node's children collection into [.destination] node | Add all nodes into [.destination] using expression leading to [.source] children | Use expression to add nodes from [.source] into [.dest]
.source
   foo1:bar1
   foo2:bar2
   foo3:bar3
.destination

// Invoking [add] with a source expression leading to all children nodes of [.source]
add:x:@.destination
   get-nodes:x:@.source/*
