// Use Hyperlambda to create an expression that finds all children nodes of [.data] having a value of 'match' | Value match expression | Expression matching value with Hyperlambda
.expression:x:@.data/*/=match
