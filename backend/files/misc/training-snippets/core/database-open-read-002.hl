// Open database called logistics and select all columns in all records from items table | Connect to logistics and select everything from items
data.connect:logistics

   // Read all records from items
   data.read
      table:items
      limit:-1
