// Use Hyperlambda to select all columns from the table called 'roles' in the database named 'magic' | Select all columns and all records from database table | Select from database
mysql.connect:magic
   mysql.select:select * from roles
