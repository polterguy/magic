// endpoint deleting from tbl1 table in db1 database record matching tbl1_id argument | delete from tbl1 where tbl1_id matches argument
.arguments
   tbl1_id:long
data.connect:db1
   data.delete
      table:tbl1
      where
         and
            tbl1_id.eq:x:@.arguments/*/tbl1_id

   // Returning message to caller
   return
      result:Record was deleted
