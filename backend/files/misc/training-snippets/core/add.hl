// Use Hyperlambda to add the [foo] node to the [.destination] node | Add a node into [.destination] | Add nodes using Hyperlambda | Append nodes to some node in Hyperlambda
.destination
add:x:@.destination
   .
      foo:bar
