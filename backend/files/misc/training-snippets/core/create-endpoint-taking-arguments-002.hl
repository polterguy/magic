// Create an endpoint taking animal, country and city as arguments | Create endpoint taking 3 strings as arguments | Endpoint taking 3 arguments
.arguments
   animal:string
   country:string
   city:string
return
   result:success
