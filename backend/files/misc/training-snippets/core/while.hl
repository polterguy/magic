
// While loop executing [.lambda] from 1 to 20 | While loop 20 times

// Loop counter.
.no:int:0

// Invoking while loop's lambda object for as long as [.no] is less than 20.
while
   lt
      get-value:x:@.no
      .:int:20
   .lambda

      // Executing this [.lambda] block 20 times.
