// Create an endpoint that takes a password argument, for then to hash it using cryptographically secure hashing, and returns the hashed password to caller
.arguments
   password:string
crypto.password.hash:x:@.arguments/*/password
return:x:@crypto.password.hash