// Regex validator | Regular expression matching alpha numeric characters | Regular expression validator matching only alpha numeric characters | Validate [.src] is alpha numeric and if not throw an exception
.src:sdfoughdf
validators.regex:x:@.src
   regex:^[a-z0-9]*$
