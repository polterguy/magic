// Use Hyperlambda to count actors in my sakila database | Execute SQL | Execute an SQL returning a scalar value | Scalar values using SQL
data.connect:sakila
   data.scalar:select count(*) from actor
