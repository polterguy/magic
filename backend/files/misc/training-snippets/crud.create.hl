// Use Hyperlambda to insert name, email and age into the tbl table in the db database | Insert new item into database | Save record to database | Create a new record in database table | Do the same as SQL insert
data.connect:db
   data.create
      table:tbl
      values
         name:John Doe
         email:john@doe.com
         age:int:67
