// Use Hyperlambda to return all regular expression matches from [.input] that is matching {match1} and {match2} | Regex matching | Hyperlambda regex
.input:Foo Bar Howdy World {match1} and {match2} and that was it
strings.matches:x:-
   .:"\\{.+?\\}"
