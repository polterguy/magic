// Set the value of [.res] to boolean true if a child node of [.data] has the name of 'foo'
.data
   nope
   jada
   foo
   bar
.res
if
   exists:x:@.data/*/foo
   .lambda
      set-value:x:@.res
         .:bool:true
