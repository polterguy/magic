
/*
 * Copies the default ".config" file to the /etc/www/ folder.
 */
if
   not
      io.file.exists:/etc/www/.config
   .lambda
      io.file.save:/etc/www/.config
         io.file.load:/misc/common-startup-files/.config