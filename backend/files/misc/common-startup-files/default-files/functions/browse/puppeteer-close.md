# Function; Puppeteer close
FUNCTION ==> puppeteer-close

Closes an existing Puppeteer session.

```plaintext
___
FUNCTION_INVOCATION[/misc/workflows/workflows/browse/puppeteer-close.hl]:
{
  "session_id": "[STRING_VALUE]"
}
___
```

Arguments:

* `session_id` is mandatory and must be a valid Puppeteer session id
