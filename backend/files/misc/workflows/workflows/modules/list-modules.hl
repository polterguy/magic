/*
 * Lists all modules in the system, returned as a list of strings.
 */
.arguments

// Ensuring user is root.
auth.ticket.verify:root

// Listing modules.
.result
io.folder.list:/modules/

// Returning modules to caller.
return-nodes:x:@io.folder.list/*
