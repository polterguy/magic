
/*
 * Creates a new module.
 */
.arguments

   // Injected by the framework.
   _session:string

   // Mandatory name of module to create. Can only contain lower case characters, numbers, underscores, and hyphens.
   module:string

// Ensuring user is root.
auth.ticket.verify:root

// Sanity checking invocation.
validators.mandatory:x:@.arguments/*/module
validators.regex:x:@.arguments/*/module
   regex:"^[a-z0-9_-]+$"

// Verifying module doesn't already exist.
io.folder.list:/modules/
for-each:x:@io.folder.list/*
   strings.split:x:@.dp/#
      .:/
   if
      eq:x:@strings.split/0/-
         get-value:x:@.arguments/*/module
      .lambda
         throw:Module already exists
            public:bool:true
            status:int:400

// Creating folder.
strings.concat
   .:/modules/
   get-value:x:@.arguments/*/module
io.folder.create:x:@strings.concat

// Creating manifest.hl file.
.manifest
   version:v1.0.0
   module_name:x:@.arguments/*/module
unwrap:x:-/*
strings.concat
   .:/modules/
   get-value:x:@.arguments/*/module
   .:/manifest.hl
lambda2hyper:x:@.manifest/*
io.file.save:x:@strings.concat
   get-value:x:@lambda2hyper

// Signaling frontend
sockets.signal:x:@.arguments/*/_session
   args
      type:floating_message
      floating_message:Module successfully created
      floating_state:success

// Returning success to caller.
return
   result:success
