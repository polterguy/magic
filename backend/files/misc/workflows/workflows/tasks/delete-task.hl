
/*
 * Deletes the specified [id] task.
 */
.arguments

   // Injected by the framework.
   _session:string

   // Mandatory id of task to delete.
   id:string

// Ensuring user is root.
auth.ticket.verify:root

// Deleting task.
tasks.delete:x:@.arguments/*/id

// Signaling frontend
sockets.signal:x:@.arguments/*/_session
   args
      type:floating_message
      floating_message:Task successfully deleted
      floating_state:success

// Returning success to caller.
return
   result:success