/*
 * Deletes the specified [filename].
 *
 * Arguments:
 *
 * - [filename] being mandatory filename to delete.
 */
.arguments

   // Injected by the framework.
   _session:string

   // Mandatory relative filename of file to delete.
   filename:string

// Ensuring user is root.
auth.ticket.verify:root

// Sanity checking invocation.
validators.mandatory:x:@.arguments/*/filename

// Prepending slash unless specified by caller.
if
   not
      strings.starts-with:x:@.arguments/*/filename
         .:/
   .lambda
      set-value:x:@.arguments/*/filename
         strings.concat
            .:/
            get-value:x:@.arguments/*/filename

// Making sure we do not delete system files.
if
   not
      or
         strings.starts-with:x:@.arguments/*/filename
            .:/etc/
         strings.starts-with:x:@.arguments/*/filename
            .:/modules/
   .lambda
      throw:"You can only delete files in the '/modules/' and '/etc/ folders'"
         status:int:400
         public:bool:true

// Verifying file exists
if
   not
      io.file.exists:x:@.arguments/*/filename
   .lambda
      throw:File does not exist
         public:bool:true
         status:int:400

// Deleting folder and returning success to caller.
io.file.delete:x:@.arguments/*/filename

// Signaling frontend
sockets.signal:x:@.arguments/*/_session
   args
      type:floating_message
      floating_message:File successfully deleted
      floating_state:success

return
   result:success
