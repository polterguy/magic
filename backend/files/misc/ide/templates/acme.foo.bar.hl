
// Creates the dynamic [acme.foo.bar] slot.
slots.create:acme.foo.bar

   // Verifies the [foo] argument was given.
   validators.mandatory:x:@.arguments/*/foo

   // Do stuff here ...

   // Return something here ...
   return:bool:true
