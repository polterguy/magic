
// Verifies the specified [token] as a valid CAPTCHA token.
.arguments
   token:string
.description:Verifies the specified [token] as a valid CAPTCHA token
.type:public

// Invoking method responsible for verifying CAPTCHA token, which will throw if not valid.
execute:magic.auth.captcha-verify
   token:x:@.arguments/*/token
