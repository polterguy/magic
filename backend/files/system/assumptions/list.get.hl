
// Returns all assumptions in system.
.description:Returns all assumptions in the system
.type:internal

// Ensures user is authorized to access endpoint.
auth.ticket.verify:root

// Listing all files in assumption test folder(s).
add:x:../*/return-nodes
   io.file.list-recursively:/misc/tests/
   io.file.list-recursively:/etc/tests/
return-nodes
