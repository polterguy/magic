/*
 * Creates a record in a MySQL table.
 */
wait.slots.signal:magic.db.mysql.create
   database:magic_auth
   table:roles
   return-id:bool:false
   values
      name:foo-bar
      description:Some description
