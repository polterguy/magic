
/*
 * Template for HTTP POST CRUD requests.
 *
 * Expects {database} and {table} [apply-file] arguments.
 */
.arguments


/*
 * Parametrizing [magic.db.mysql.create].
 */
add:x:./*/signal/*/values
   get-nodes:x:@.arguments/*


/*
 * Invoking [magic.db.mysql.create]
 */
signal:magic.db.mysql.create
   database:{database}
   table:{table}
   values


/*
 * Returning results back to caller.
 */
return-nodes:x:@signal/*
