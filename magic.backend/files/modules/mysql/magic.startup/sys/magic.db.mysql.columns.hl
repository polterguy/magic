
/*
 * Lists all columns in your [database]/[table] MySQL table.
 */
slot:magic.db.mysql.columns
   .arguments
      database:string
      table:string
   .lambda


      /*
       * Opens up our connection
       */
      mysql.connect:x:@.arguments/*/database


         concat
            :"describe "
            :'`'
            get-value:x:@.arguments/*/table
            :'`'
         mysql.select:x:@concat
         return-nodes:x:-/*
