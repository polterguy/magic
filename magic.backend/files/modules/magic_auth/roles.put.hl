.arguments
   id:long
   name:string
auth.ticket.verify:root
add:x:./*/slots.signal/*/values
   get-nodes:x:@.arguments/*
unwrap:x:+/*/ids/*
slots.signal:magic.db.mysql.update
   database:magic_auth
   table:roles
   values
   ids
      id:x:@.arguments/*/id
slots.return-nodes:x:@slots.signal/*
