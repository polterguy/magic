.arguments
   limit:long
   offset:long
   order:string
   direction:string
   RegionID:long
   RegionDescription:string
auth.ticket.verify:root
add:x:./*/*slots.signal/*/args
   get-nodes:x:@.arguments/*
*slots.signal:magic.db.mssql.read
   database:northwind
   table:dbo.Region
   args
   columns
      RegionID
      RegionDescription
slots.return-nodes:x:@*slots.signal/*
