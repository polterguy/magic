.arguments
   RegionID:long
auth.ticket.verify:root
add:x:./*/*slots.signal/*/ids
   get-nodes:x:@.arguments/*
*slots.signal:magic.db.mssql.delete
   database:northwind
   table:dbo.Region
   ids
slots.return-nodes:x:@*slots.signal/*
