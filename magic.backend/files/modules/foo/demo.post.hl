.arguments
   text:string
auth.ticket.verify:root
validators.email:x:@.arguments/*/text
add:x:./*/wait.slots.signal/*/values
   get-nodes:x:@.arguments/*
wait.slots.signal:magic.db.mysql.create
   database:foo
   table:demo
   return-id:bool:true
   values
slots.return-nodes:x:@wait.slots.signal/*
