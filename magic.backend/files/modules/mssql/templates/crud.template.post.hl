
/*
 * Template for HTTP POST CRUD requests.
 *
 * Expects {database} and {table} [apply] arguments.
 */
.arguments


/*
 * Parametrizing [magic.db.mssql.create].
 */
add:x:./*/slots.signal/*/values
   get-nodes:x:@.arguments/*


/*
 * Invoking [magic.db.mssql.create]
 */
slots.signal:magic.db.mssql.create
   database:{database}
   table:{table}
   values


/*
 * Returning results back to caller.
 */
slots.return-nodes:x:@slots.signal/*
