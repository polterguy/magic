.arguments
   limit:long
   offset:long
   order:string
   direction:string
   id:string
   contact_id:string
   quote_id:string
   contact_role:string
   date_modified:date
   deleted:long
   id.like:string
   id.mt:string
   id.lt:string
   id.mteq:string
   id.lteq:string
   id.neq:string
   id.eq:string
   contact_id.like:string
   contact_id.mt:string
   contact_id.lt:string
   contact_id.mteq:string
   contact_id.lteq:string
   contact_id.neq:string
   contact_id.eq:string
   quote_id.like:string
   quote_id.mt:string
   quote_id.lt:string
   quote_id.mteq:string
   quote_id.lteq:string
   quote_id.neq:string
   quote_id.eq:string
   contact_role.like:string
   contact_role.mt:string
   contact_role.lt:string
   contact_role.mteq:string
   contact_role.lteq:string
   contact_role.neq:string
   contact_role.eq:string
   date_modified.mt:date
   date_modified.lt:date
   date_modified.mteq:date
   date_modified.lteq:date
   date_modified.neq:date
   date_modified.eq:date
   deleted.mt:long
   deleted.lt:long
   deleted.mteq:long
   deleted.lteq:long
   deleted.neq:long
   deleted.eq:long
auth.ticket.verify:root
lambda2hyper:x:@.arguments/*
.log
strings.concat
   get-value:x:@.log
   .:@" - Arguments: 
"
   get-value:x:@lambda2hyper
log.info:x:@strings.concat
add:x:./*/wait.slots.signal/*/args
   get-nodes:x:@.arguments/*
wait.slots.signal:magic.db.mysql.read
   database:sugarcrm
   table:quotes_contacts
   args
   columns
      id
      contact_id
      quote_id
      contact_role
      date_modified
      deleted
slots.return-nodes:x:@wait.slots.signal/*
