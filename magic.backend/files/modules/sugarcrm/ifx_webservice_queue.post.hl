.arguments
   scope:string
   field_id:string
   field1:string
   field2:string
   added_datetime:null
   last_tried_datetime:string
   last_result:string
   response:string
   success:long
   count:long
auth.ticket.verify:root
lambda2hyper:x:@.arguments/*
.log
strings.concat
   get-value:x:@.log
   .:@" - Arguments: 
"
   get-value:x:@lambda2hyper
log.info:x:@strings.concat
add:x:./*/wait.slots.signal/*/values
   get-nodes:x:@.arguments/*
wait.slots.signal:magic.db.mysql.create
   database:sugarcrm
   table:ifx_webservice_queue
   return-id:bool:true
   values
slots.return-nodes:x:@wait.slots.signal/*
