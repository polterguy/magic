.arguments
   limit:long
   offset:long
   order:string
   direction:string
   field:string
   value:string
   field.like:string
   field.mt:string
   field.lt:string
   field.mteq:string
   field.lteq:string
   field.neq:string
   field.eq:string
   value.like:string
   value.mt:string
   value.lt:string
   value.mteq:string
   value.lteq:string
   value.neq:string
   value.eq:string
auth.ticket.verify:root
lambda2hyper:x:@.arguments/*
.log
strings.concat
   get-value:x:@.log
   .:@" - Arguments: 
"
   get-value:x:@lambda2hyper
log.info:x:@strings.concat
add:x:./*/wait.slots.signal/*/args
   get-nodes:x:@.arguments/*
wait.slots.signal:magic.db.mysql.read
   database:sugarcrm
   table:ifx_webservice_settings
   args
   columns
      field
      value
slots.return-nodes:x:@wait.slots.signal/*
