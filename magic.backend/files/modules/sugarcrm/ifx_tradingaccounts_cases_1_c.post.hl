.arguments
   id:string
   date_modified:date
   deleted:long
   ifx_tradingaccounts_cases_1ifx_tradingaccounts_ida:string
   ifx_tradingaccounts_cases_1cases_idb:string
auth.ticket.verify:root
lambda2hyper:x:@.arguments/*
.log
strings.concat
   get-value:x:@.log
   .:@" - Arguments: 
"
   get-value:x:@lambda2hyper
log.info:x:@strings.concat
add:x:./*/wait.slots.signal/*/values
   get-nodes:x:@.arguments/*
wait.slots.signal:magic.db.mysql.create
   database:sugarcrm
   table:ifx_tradingaccounts_cases_1_c
   return-id:bool:false
   values
slots.return-nodes:x:@wait.slots.signal/*
