.arguments
   team_id:string
   team_set_id:string
   id:string
   deleted:long
   date_entered:date
   date_modified:date
   modified_user_id:string
   created_by:string
   name:string
   bundle_stage:string
   description:string
   tax:decimal
   tax_usdollar:decimal
   total:decimal
   total_usdollar:decimal
   subtotal_usdollar:decimal
   shipping_usdollar:decimal
   deal_tot:decimal
   deal_tot_usdollar:decimal
   new_sub:decimal
   new_sub_usdollar:decimal
   subtotal:decimal
   shipping:decimal
   currency_id:string
auth.ticket.verify:root
lambda2hyper:x:@.arguments/*
.log
strings.concat
   get-value:x:@.log
   .:@" - Arguments: 
"
   get-value:x:@lambda2hyper
log.info:x:@strings.concat
add:x:./*/wait.slots.signal/*/values
   get-nodes:x:@.arguments/*
wait.slots.signal:magic.db.mysql.create
   database:sugarcrm
   table:product_bundles
   return-id:bool:false
   values
slots.return-nodes:x:@wait.slots.signal/*
