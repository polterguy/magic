.arguments
   limit:long
   offset:long
   order:string
   direction:string
   id:string
   quote_id:string
   account_id:string
   account_role:string
   date_modified:date
   deleted:long
   id.like:string
   id.mt:string
   id.lt:string
   id.mteq:string
   id.lteq:string
   id.neq:string
   id.eq:string
   quote_id.like:string
   quote_id.mt:string
   quote_id.lt:string
   quote_id.mteq:string
   quote_id.lteq:string
   quote_id.neq:string
   quote_id.eq:string
   account_id.like:string
   account_id.mt:string
   account_id.lt:string
   account_id.mteq:string
   account_id.lteq:string
   account_id.neq:string
   account_id.eq:string
   account_role.like:string
   account_role.mt:string
   account_role.lt:string
   account_role.mteq:string
   account_role.lteq:string
   account_role.neq:string
   account_role.eq:string
   date_modified.mt:date
   date_modified.lt:date
   date_modified.mteq:date
   date_modified.lteq:date
   date_modified.neq:date
   date_modified.eq:date
   deleted.mt:long
   deleted.lt:long
   deleted.mteq:long
   deleted.lteq:long
   deleted.neq:long
   deleted.eq:long
auth.ticket.verify:root
lambda2hyper:x:@.arguments/*
.log
strings.concat
   get-value:x:@.log
   .:@" - Arguments: 
"
   get-value:x:@lambda2hyper
log.info:x:@strings.concat
add:x:./*/wait.slots.signal/*/args
   get-nodes:x:@.arguments/*
wait.slots.signal:magic.db.mysql.read
   database:sugarcrm
   table:quotes_accounts
   args
   columns
      id
      quote_id
      account_id
      account_role
      date_modified
      deleted
slots.return-nodes:x:@wait.slots.signal/*
