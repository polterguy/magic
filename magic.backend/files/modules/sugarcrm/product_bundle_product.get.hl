.arguments
   limit:long
   offset:long
   order:string
   direction:string
   id:string
   date_modified:date
   deleted:long
   bundle_id:string
   product_id:string
   product_index:long
   id.like:string
   id.mt:string
   id.lt:string
   id.mteq:string
   id.lteq:string
   id.neq:string
   id.eq:string
   date_modified.mt:date
   date_modified.lt:date
   date_modified.mteq:date
   date_modified.lteq:date
   date_modified.neq:date
   date_modified.eq:date
   deleted.mt:long
   deleted.lt:long
   deleted.mteq:long
   deleted.lteq:long
   deleted.neq:long
   deleted.eq:long
   bundle_id.like:string
   bundle_id.mt:string
   bundle_id.lt:string
   bundle_id.mteq:string
   bundle_id.lteq:string
   bundle_id.neq:string
   bundle_id.eq:string
   product_id.like:string
   product_id.mt:string
   product_id.lt:string
   product_id.mteq:string
   product_id.lteq:string
   product_id.neq:string
   product_id.eq:string
   product_index.mt:long
   product_index.lt:long
   product_index.mteq:long
   product_index.lteq:long
   product_index.neq:long
   product_index.eq:long
auth.ticket.verify:root
lambda2hyper:x:@.arguments/*
.log
strings.concat
   get-value:x:@.log
   .:@" - Arguments: 
"
   get-value:x:@lambda2hyper
log.info:x:@strings.concat
add:x:./*/wait.slots.signal/*/args
   get-nodes:x:@.arguments/*
wait.slots.signal:magic.db.mysql.read
   database:sugarcrm
   table:product_bundle_product
   args
   columns
      id
      date_modified
      deleted
      bundle_id
      product_id
      product_index
slots.return-nodes:x:@wait.slots.signal/*
