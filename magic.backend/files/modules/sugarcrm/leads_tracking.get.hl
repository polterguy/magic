.arguments
   limit:long
   offset:long
   order:string
   direction:string
   id:long
   lead_id:string
   date_created:date
   tracking:string
   id.mt:long
   id.lt:long
   id.mteq:long
   id.lteq:long
   id.neq:long
   id.eq:long
   lead_id.like:string
   lead_id.mt:string
   lead_id.lt:string
   lead_id.mteq:string
   lead_id.lteq:string
   lead_id.neq:string
   lead_id.eq:string
   date_created.mt:date
   date_created.lt:date
   date_created.mteq:date
   date_created.lteq:date
   date_created.neq:date
   date_created.eq:date
   tracking.like:string
   tracking.mt:string
   tracking.lt:string
   tracking.mteq:string
   tracking.lteq:string
   tracking.neq:string
   tracking.eq:string
auth.ticket.verify:root
lambda2hyper:x:@.arguments/*
.log
strings.concat
   get-value:x:@.log
   .:@" - Arguments: 
"
   get-value:x:@lambda2hyper
log.info:x:@strings.concat
add:x:./*/wait.slots.signal/*/args
   get-nodes:x:@.arguments/*
wait.slots.signal:magic.db.mysql.read
   database:sugarcrm
   table:leads_tracking
   args
   columns
      id
      lead_id
      date_created
      tracking
slots.return-nodes:x:@wait.slots.signal/*
