.arguments
   limit:long
   offset:long
   order:string
   direction:string
   id:string
   contact_id:string
   opportunity_id:string
   contact_role:string
   date_modified:date
   deleted:long
   id.like:string
   id.mt:string
   id.lt:string
   id.mteq:string
   id.lteq:string
   id.neq:string
   id.eq:string
   contact_id.like:string
   contact_id.mt:string
   contact_id.lt:string
   contact_id.mteq:string
   contact_id.lteq:string
   contact_id.neq:string
   contact_id.eq:string
   opportunity_id.like:string
   opportunity_id.mt:string
   opportunity_id.lt:string
   opportunity_id.mteq:string
   opportunity_id.lteq:string
   opportunity_id.neq:string
   opportunity_id.eq:string
   contact_role.like:string
   contact_role.mt:string
   contact_role.lt:string
   contact_role.mteq:string
   contact_role.lteq:string
   contact_role.neq:string
   contact_role.eq:string
   date_modified.mt:date
   date_modified.lt:date
   date_modified.mteq:date
   date_modified.lteq:date
   date_modified.neq:date
   date_modified.eq:date
   deleted.mt:long
   deleted.lt:long
   deleted.mteq:long
   deleted.lteq:long
   deleted.neq:long
   deleted.eq:long
auth.ticket.verify:root
lambda2hyper:x:@.arguments/*
.log
strings.concat
   get-value:x:@.log
   .:@" - Arguments: 
"
   get-value:x:@lambda2hyper
log.info:x:@strings.concat
add:x:./*/wait.slots.signal/*/args
   get-nodes:x:@.arguments/*
wait.slots.signal:magic.db.mysql.read
   database:sugarcrm
   table:opportunities_contacts
   args
   columns
      id
      contact_id
      opportunity_id
      contact_role
      date_modified
      deleted
slots.return-nodes:x:@wait.slots.signal/*
