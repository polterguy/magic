.arguments
   limit:long
   offset:long
   order:string
   direction:string
   id:string
   date_modified:date
   deleted:long
   bundle_id:string
   quote_id:string
   bundle_index:long
   id.like:string
   id.mt:string
   id.lt:string
   id.mteq:string
   id.lteq:string
   id.neq:string
   id.eq:string
   date_modified.mt:date
   date_modified.lt:date
   date_modified.mteq:date
   date_modified.lteq:date
   date_modified.neq:date
   date_modified.eq:date
   deleted.mt:long
   deleted.lt:long
   deleted.mteq:long
   deleted.lteq:long
   deleted.neq:long
   deleted.eq:long
   bundle_id.like:string
   bundle_id.mt:string
   bundle_id.lt:string
   bundle_id.mteq:string
   bundle_id.lteq:string
   bundle_id.neq:string
   bundle_id.eq:string
   quote_id.like:string
   quote_id.mt:string
   quote_id.lt:string
   quote_id.mteq:string
   quote_id.lteq:string
   quote_id.neq:string
   quote_id.eq:string
   bundle_index.mt:long
   bundle_index.lt:long
   bundle_index.mteq:long
   bundle_index.lteq:long
   bundle_index.neq:long
   bundle_index.eq:long
auth.ticket.verify:root
lambda2hyper:x:@.arguments/*
.log
strings.concat
   get-value:x:@.log
   .:@" - Arguments: 
"
   get-value:x:@lambda2hyper
log.info:x:@strings.concat
add:x:./*/wait.slots.signal/*/args
   get-nodes:x:@.arguments/*
wait.slots.signal:magic.db.mysql.read
   database:sugarcrm
   table:product_bundle_quote
   args
   columns
      id
      date_modified
      deleted
      bundle_id
      quote_id
      bundle_index
slots.return-nodes:x:@wait.slots.signal/*
