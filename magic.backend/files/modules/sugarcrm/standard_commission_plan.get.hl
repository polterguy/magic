.arguments
   limit:long
   offset:long
   order:string
   direction:string
   idstandard_commission_plan:long
   symbol:string
   category:string
   rebate:string
   pips_usd:string
   idstandard_commission_plan.mt:long
   idstandard_commission_plan.lt:long
   idstandard_commission_plan.mteq:long
   idstandard_commission_plan.lteq:long
   idstandard_commission_plan.neq:long
   idstandard_commission_plan.eq:long
   symbol.like:string
   symbol.mt:string
   symbol.lt:string
   symbol.mteq:string
   symbol.lteq:string
   symbol.neq:string
   symbol.eq:string
   category.like:string
   category.mt:string
   category.lt:string
   category.mteq:string
   category.lteq:string
   category.neq:string
   category.eq:string
   rebate.like:string
   rebate.mt:string
   rebate.lt:string
   rebate.mteq:string
   rebate.lteq:string
   rebate.neq:string
   rebate.eq:string
   pips_usd.like:string
   pips_usd.mt:string
   pips_usd.lt:string
   pips_usd.mteq:string
   pips_usd.lteq:string
   pips_usd.neq:string
   pips_usd.eq:string
auth.ticket.verify:root
lambda2hyper:x:@.arguments/*
.log
strings.concat
   get-value:x:@.log
   .:@" - Arguments: 
"
   get-value:x:@lambda2hyper
log.info:x:@strings.concat
add:x:./*/wait.slots.signal/*/args
   get-nodes:x:@.arguments/*
wait.slots.signal:magic.db.mysql.read
   database:sugarcrm
   table:standard_commission_plan
   args
   columns
      idstandard_commission_plan
      symbol
      category
      rebate
      pips_usd
slots.return-nodes:x:@wait.slots.signal/*
