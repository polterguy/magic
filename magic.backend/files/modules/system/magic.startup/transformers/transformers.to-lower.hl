
/*
 * Returns the lower case version of the first given input argument, with the same
 * name, but only lower case characters in its value.
 */
slots.create:transformers.to-lower

   set-value:x:@.arguments/0/#
      strings.to-lower:x:@.arguments/0/#
