
/*
 * Template for HTTP GET CRUD requests.
 */
.arguments
   limit:long
   offset:long
   order:string
   direction:string


/*
 * Parametrizing [magic.db.mysql.read].
 */
add:x:./*/signal/*/args
   get-nodes:x:@.arguments/*


/*
 * Invoking [magic.db.mysql.read], making sure we unwrap the [id].
 */
signal:magic.db.mysql.read
   database:{database}
   table:{table}
   args
   columns


/*
 * Returning results back to caller.
 */
return-nodes:x:@signal/*
