
/*
 * Template for deleting items.
 */
.arguments:{args}


/*
 * Retrieving connection string according to database constant.
 */
signal:magic.db.mysql.connection
   database:{database}


/*
 * Invoking [magic.db.mysql.delete], making sure we unwrap the [id].
 */
add:x:+/+/*/ids
   get-nodes:x:/@.arguments/*
unwrap:x:+/*/connection
signal:magic.db.mysql.delete
   connection:x:./@signal
   table:{table}
   ids


/*
 * Returning results back to caller.
 */
return-nodes:x:@signal/*
