
/*
 * Returns metadata for specified endpoint.
 *
 * Arguments
 *
 * - [url] - Mandatory, url of endpoint to retrieve meta data about
 * - [verb] - Mandatory, HTTP verb of endpoint to retrieve meta data about
 */
.arguments
   url:string
   verb:string


/*
 * Ensures user is authorized to access endpoint.
 */
auth.ticket.verify:root


endpoints.get-arguments
   url:x:@.arguments/*/url
   verb:x:@.arguments/*/verb
slots.return-nodes:x:-/*
