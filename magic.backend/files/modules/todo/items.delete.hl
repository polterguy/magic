
/*
 * Deletes the specified [id] TODO item.
 */


/*
 * Invoking [magic.db.delete], making sure we unwrap the [id].
 */
unwrap:x:./*/signal/*/ids/*
signal:magic.db.delete
   connection:signal:@"config:""Databases:MySQLConnection"""
   table:items
   ids
      id:x:@.arguments/*/id


/*
 * Returning results back to caller.
 */
return-nodes:x:@signal/*
