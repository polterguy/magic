
/*
 * Returns items from your TODO table.
 */


set-value:x:./*/signal/*/where/*/id
   convert:x:@.arguments/*/id
      type:long
remove-node:x:@.arguments/*/id
add:x:./*/signal/*/values
   get-nodes:x:@.arguments/*
signal:magic.db.update
   connection:signal:@"config:""Databases:MySQLConnection"""
   table:items
   values
   where
      id
return-nodes:x:-/*
