
# Docker file to couple frontend, backend, and MySQL database instance into one.

version: "3.3"

services:

  db:
    image: mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: ThisIsNotAGoodPassword
    volumes:
      - database:/var/lib/mysql

  backend:
    image: servergardens/magic-backend:latest
    depends_on:
      - db
    restart: always
    ports:
      - "4444:4444"
    volumes:
      - etc_folder:/magic/files/etc
      - misc_folder:/magic/files/misc
      - modules_folder:/magic/files/modules
      - temp_folder:/magic/files/temp
      - config_folder:/magic/config

  frontend:
    image: servergardens/magic-frontend:latest
    depends_on:
      - backend
    restart: always
    ports:
      - "5555:80"

volumes:
  database:
  etc_folder:
  misc_folder:
  modules_folder:
  temp_folder:
  config_folder:
